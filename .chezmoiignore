# Rust build artifacts under any src tree (e.g. dot_local/src/shake-to-find)
# Cargo.lock は現状 dev 段階で固定していない。依存 pin が必要になったら、
# ここの `**/Cargo.lock` を外して該当クレート側で個別に管理する。
**/target/
**/Cargo.lock

# Node.js dependencies (e.g. dot_claude/skills/polish-ja/textlint/node_modules)
# package-lock.json は依存固定のため同梱する
**/node_modules/
